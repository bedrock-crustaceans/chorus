[package]
name = "rastra-utils"
version.workspace = true
description = "Util classes needed for RAstra"
edition = "2021"

[dependencies]
rastra-api = { path = "../rastra-api" }

log = "0.4"
fern = "0.7"
fern_colored = { version = "0.6", features = ["colored"] }
chrono = "0.4"
rand = "0.8"